import styled from "styled-components";

export const PlayerDeckContainer = styled.section`
  border-radius: 5px;

  padding: 1em;
  height: 30ch;
  margin: 1em 0;

  background: var(--chakra-colors-gray-900);
  /* box-shadow: 0 8px 32px 0 var(--chakra-colors-gray-900);
  border: 1px solid var(--chakra-colors-teal-600);
  background-image: linear-gradient(
      var(--chakra-colors-teal-600) 0.1em,
      transparent 0.1em
    ),
    linear-gradient(
      90deg,
      var(--chakra-colors-teal-600) 0.1em,
      transparent 0.1em
    );
  background-size: 30ch 15ch; */
`;
